# Runner Crash Containment Design

**Date:** 2026-08-31
**Status:** Revised after formal `/advice` review; implementation not started
**Primary goal:** Keep the physical Ubuntu host, desktop session, Warp, and operator tools responsive while ten Linux GitHub Actions runners execute concurrently.

## Problem Statement

The production fleet currently runs ten runner containers through the host Docker daemon. Each container has a finite memory, CPU, and PID limit, but the shared host `actions.slice` has infinite aggregate limits. The tracked finite `actions.slice` is installed only inside Colima, so it does not constrain host-Docker containers. At the same time, a local `agents.slice` drop-in overrides the tracked finite values with infinity, while Ubuntu's vendor `user@.service` policy monitors the entire user manager for memory pressure.

That topology inverts the intended failure order. During runner refill bursts, `systemd-oomd` can kill the deploy owner's `user@UID.service`, taking Warp, tmux, Codex, and the desktop session together, while the runner aggregate remains unbounded. Colima is not the root requirement: the availability requirement is that lower-priority workloads fail before the operator session and physical host.

## Goals

1. Keep exactly ten Linux runner slots configured and available; reducing runner count is not an accepted containment mechanism.
2. Make runner memory, CPU, swap, and process use finite in one host-level aggregate in addition to existing per-container limits.
3. Make interactive agent and automation aggregates finite so they cannot consume the host reserve.
4. Ensure runner and agent pressure is handled inside explicit workload roots while the desktop user manager is not a direct `systemd-oomd` monitoring root.
5. Refuse new runner admission when required containment is missing or infinite.
6. Keep every production control, installer, verifier, and policy statement git-tracked and portable to a fresh Ubuntu machine.
7. Preserve operator-only authority over physical-host reboot, shutdown, VM teardown, and other broad lifecycle actions.

## Non-Goals

- Containing deliberately malicious workflow code or kernel exploits. Host Docker shares the host kernel and is accepted for this trusted-workload threat model.
- Making Colima a required Linux backend.
- Adding another watchdog, polling daemon, or automatic host/VM restart loop.
- Reducing the ten-runner Linux capacity contract.
- Reworking runner registration, GitHub API reconciliation, or per-job lifecycle behavior.

## Assumptions and Recommended Defaults

| Question considered | Auto-picked answer | Rationale |
|---|---|---|
| Is the primary threat malicious workflow code or resource exhaustion? | Resource exhaustion and host responsiveness. | The operator explicitly prioritized crash containment over security isolation. |
| Must Linux use Colima? | No; use host Docker when its resource envelope is proven. | Colima adds QEMU memory and KVM boot failure modes without replacing the need for host cgroups. |
| May containment reduce runner count? | No; retain ten Linux runners. | Ten runners are an explicit capacity requirement. |
| What should die first under runner pressure? | An eligible descendant of the monitored runner root; the desktop user manager is not a direct pressure target. | `systemd-oomd` selects within each monitored root, so the design guarantees workload-local candidate selection but does not claim a total order across independent runner, agent, and automation roots. |
| How much host RAM remains outside hard workload caps? | Budget about 8.5 GiB on the measured 62.48-GiB host. | `actions.slice` 28 GiB + `agents.slice` 20 GiB + `automation.slice` 6 GiB = 54 GiB, leaving about 8.48 GiB. This is headroom for uncapped desktop, kernel, Docker, and system use, not a proof that global OOM is impossible. |
| What initial runner aggregate limits apply? | `MemoryHigh=26G`, `MemoryMax=28G`, `MemorySwapMax=0`, `TasksMax=6000`, `CPUQuota=2000%`. | Ten 2500-MiB container limits total about 24.4 GiB, so 26 GiB avoids permanent reclaim at full declared capacity. Ten 2-CPU container limits total 20 CPUs, so the aggregate quota preserves the declared fleet sum while reserving 12 of 32 logical CPUs for the host. |
| Does this lower the live per-runner memory limit? | No; Jeff-Ubuntu already has `count=10`, `runner_floor_mb=2500`, and `limits.memory_mb=2500`. | The tracked Linux example still says 3000 MiB. Aligning it to the deployed 2500-MiB profile changes the fresh-machine default by 16.7%, so the release gate records job outcomes and refuses to silently lower any live host that differs. |
| What initial agent aggregate limits apply? | `MemoryHigh=18G`, `MemoryMax=20G`, `MemorySwapMax=2G`, `TasksMax=8192`. | Fresh read-only cgroup evidence was about 16.26 GiB unadjusted `memory.current` and 17.14 GiB peak, with about 1.1 GiB anon+kernel and most of the remainder reclaimable file cache. The high boundary preserves the required 1-GiB margin against enforced current usage; the hard cap prevents another 36.7-GiB single-agent event. |
| What host hardware may use this fixed profile? | At least 62 GiB RAM and 32 logical CPUs. | The numeric budgets are sized for the current measured 62.48-GiB, 32-thread host; a smaller machine must fail closed until it has a reviewed profile. |
| Which runner labels are deployed and selected? | The current live fleet has four labels; the proposed/future deployed runners advertise those four plus `Linux` and `X64`. | The capacity proof selects `[self-hosted, self-hosted-mikey, ezgha, ez-runner-c]` only after a registry-wide query proves that subset matches exactly the expected ten runners and no collision. |
| How is runner I/O contained portably? | `IOWeight=25` when the cgroup I/O controller is available; no guessed bandwidth cap. | Relative weight preserves interactive priority without hard-coding a device-specific throughput value. |
| Should an unavailable Colima VM fall back to host Docker? | Backend selection may use host Docker only when host containment passes; missing containment must fail closed. | Availability must not be restored by silently deleting the resource boundary. |
| Should another monitor repair failures? | No. | Previous monitors and watchdogs created competing mutation paths; installation, startup preflight, and verification are sufficient. |
| How broad should this change be? | Policy, systemd artifacts, one assertion/installer path, a narrow Rust topology/admission seam, and existing diagnostics. | This is the smallest scope that makes the live boundary tracked and enforceable; runner scheduling remains unchanged except for requiring the opaque admission capability at existing mutation boundaries. |
| How should implementation be parallelized? | Parallelize disjoint policy, systemd, preflight, and verifier lanes; use one integration owner for shared installer and final deployment. | This follows `/parallel` while preserving single-writer ownership of the live fleet and shared files. |

## Approaches Considered

### 1. Host Docker with a fail-closed host resource envelope (recommended)

Keep the existing host Docker execution path. Add a real system-level `actions.slice`, directly enroll workload slices with `systemd-oomd`, disable broad monitoring of `user@.service`, and require a startup preflight before runner admission.

This has the fewest runtime layers, avoids QEMU overhead, and directly fixes the observed failure: unbounded workload aggregates combined with a desktop-wide OOM target. It does not provide a separate kernel security boundary, which is outside this design's threat model.

### 2. Colima-only Linux runners

Repair `/dev/kvm` boot ordering, converge the legacy and canonical Colima profiles, remove writable host-home mounts, size QEMU, and run Docker inside the VM. This provides a stronger security boundary, but QEMU remains a large host process requiring the same cgroup/OOM policy. It also introduces VM boot, disk, socket, and lifecycle failure modes that do not advance the primary crash-containment goal.

### 3. Colima primary with host-Docker fallback

Prefer Colima but silently switch to host Docker when the VM is unavailable. This is rejected. The August 27 host-Docker drop-in was created immediately after repeated Colima failures and bypassed the only tracked aggregate limit. A topology-changing fallback makes the active safety contract unknowable during an incident.

## Architecture

### Resource hierarchy

The host uses three workload aggregates with non-overlapping ownership:

```text
-.slice
├── actions.slice                 system scope, Docker container scopes
│   └── docker-*.scope            one ephemeral runner per scope
├── ezghaproof.slice              system scope, synthetic proofs only
│   ├── ezghaproof-hardlimit.slice
│   └── ezghaproof-oomd.slice
├── user.slice
│   └── user-1000.slice
│       └── user@UID.service
│           ├── agents.slice      interactive coding agents and descendants
│           ├── automation.slice  AO and factory daemons
│           ├── app.slice         Warp and desktop applications
│           └── session.slice     GNOME session services
└── system.slice                  Docker daemon and host services
```

`actions.slice` is a root-owned system slice because Docker creates container scopes through the system manager. Installing a user-scoped unit with the same name does not constrain those scopes and is forbidden as evidence of containment.

### OOM ownership

- `actions.slice`, `agents.slice`, and `automation.slice` set `ManagedOOMMemoryPressure=kill` with an explicit 50% pressure limit.
- Tracked system drop-ins force both `ManagedOOMMemoryPressure=auto` and `ManagedOOMSwap=auto` on `-.slice`, `user.slice`, the deploy owner's `user-UID.slice`, `user@UID.service`, and the desktop app/session ancestors. The broad `user@UID.service` also resets `ManagedOOMPreference=none` and `OOMScoreAdjust=0`; setting it to `omit` or assigning a negative score is forbidden because that protects the entire session subtree and can displace kernel/oomd victims rather than containing the source. `ManagedOOMMemoryPressureLimit=0%` is not treated as protection because systemd ignores that property unless pressure mode is `kill`. This explicitly removes the vendor `user@UID.service` kill target that formed the previous crash blast radius: Warp, tmux, Codex, and the desktop manager must not be selected as one unit. Only finite workload roots (`actions.slice`, `agents.slice`, and `automation.slice`, plus an active proof root) may appear as `systemd-oomd` kill roots in `oomctl`.
- Hard `MemoryMax` limits remain the deterministic last boundary even if `systemd-oomd` is unavailable or slow.
- No workload receives `OOMScoreAdjust=-1000`. Protected workload exemptions invert the intended kill order and are removed from the host-Docker topology.
- Supported agent launchers have no environment escape hatch around `agents.slice`; every detected supported agent CLI process must be a descendant of the finite aggregate before deployment proceeds. Existing unscoped processes are reported and allowed to exit or be relaunched, never killed automatically.
- Independent monitored roots have no global priority order. The contract is local: an `actions.slice` pressure action chooses an eligible runner descendant, while agent and automation pressure is handled inside their own roots.
- `MemorySwapMax=0` prevents runner swap thrash but can make pressure rise faster than `systemd-oomd` reacts. Kernel hard limits are therefore the final containment mechanism, not a claim of deterministic oomd timing.

### Admission permit

Rust owns a short-lived opaque `AdmissionPermit` through one canonical containment implementation. `ezgha containment-check --install-static --json` exposes a read-only verdict for the mode-`0600` proposed endpoint, hardware, deployment state, privilege, current use where applicable, and rollback storage without requiring a reachable VM daemon. For an explicitly configured recoverable Colima/Lima endpoint only, that verdict may authorize the separate exact-backend recovery decision. `ezgha containment-check --install-dynamic --json` then exposes a read-only verdict for reachable endpoint topology, Docker's cgroup driver, and `oomctl`; both install verdicts must pass before image work but neither returns an admission capability. `--admission` exposes the stricter runtime verdict after effective installed cgroup/oomd policy and every final deployment-receipt artifact pass. Internal Rust callers use that same implementation to obtain an in-memory permit with a private scope and expiry. `RuntimeAdmission` authorizes only the established runtime mutation paths after final installed-state validation. Private `TransactionAdmission` is callable only by deferred service installation with a validated inherited transaction-lock FD and candidate directory; it validates every non-unit receipt artifact at its final path and both unit candidates at their receipt-bound staged paths, and authorizes only atomic installation, verification, and manager reload for those two units. It cannot reach service start, pre-pull, reconciliation, registration, or runner creation. The CLI JSON never serializes or accepts either permit, so an environment variable, CLI argument, or shell-generated JSON cannot forge one.

Every Linux runner-resource or external control-plane side effect is typed at its call site as either `AdmissionMutation` or `ReliefCleanup`; untyped Docker, GitHub, runner-ledger, quarantine, workspace, or runner-control writes are forbidden. A third type, non-capability `AuditStateWrite`, owns only bounded local audit/history, alert, and failure-counter persistence. Its API cannot reach Docker, GitHub, runner ledgers, quarantine/resource mutation, retry, dispatch, dead-man credit, or permit issuance. Direct `start`, direct `serve` before pre-pull, direct `install-service`, and direct `canary-once` perform the full bounded check synchronously. Long-lived `serve` performs one full check before pre-pull, then owns an `AttestationSupervisor` with exactly one read-only full check in flight. A new run starts no later than 20 seconds after the preceding completed run and has its own 15-second wall-clock deadline including child processes. It checks Docker, `systemctl`, `oomctl`, config, receipt, SHA-256 digests, endpoint/topology, fixed count, cgroup state, and policy, then atomically publishes a non-capability `RuntimeAttestation` containing pass/failure class, observation generation, revocation epoch, completion instant, boot ID, normalized endpoint, persisted count, and receipt/config metadata identities. A successful snapshot expires 45 seconds after completion. The reconciliation thread never invokes or joins those probes and never charges them to its 30-second cadence or the shared 75-second queue-monitor budget.

`AdmissionMutation` covers every automatic service-loop slot reserve, runner-ID record or release, quarantine write, workspace reset, Docker pull/run/rm, GitHub JIT POST, runner DELETE, workflow cancel/force-cancel, canary dispatch, and stale/zombie reconciliation side effect. `ensure_count`, the reaper, and the canary scheduler receive an `AdmissionIssuer`; immediately before each atomic local commit or actual child-process spawn, the owning call site invokes subprocess-free `mint_mutation()`. A one-use `TickAdmission` never authorizes a batch, callback, retry loop, sleep, or second child. Retrying GitHub helpers receive a private issuer callback and mint/assert immediately before every POST or DELETE attempt, including every backoff retry and each JIT conflict-delete-retry command. Read-only list, inspect, probe, and poll operations need no permit.

Minting reads the latest successful snapshot, rechecks boot ID plus receipt/config metadata identity, and returns a one-use permit bound to the current revocation epoch with expiry at the earlier of five seconds after issue or snapshot expiry. `assert_current()` runs immediately before its single mutation. A newer successful observation generation does not change the revocation epoch or invalidate a live permit. `PolicyFailed`, `StaleSince`, boot-ID change, or metadata mismatch increments the epoch and revokes all older permits. A serialized ten-runner refill may therefore remint from successive healthy snapshots for more than 300 seconds, while drift between any two automatic side effects blocks the next one.

`ReliefCleanup` is deliberately narrower and does not require healthy admission. Explicit `stop`, SIGTERM drain, `emergency-stop --confirm [--cancel-running]`, and compensation for one previously admitted JIT transition may remove only exact proven-owned local containers, registrations, and ledger rows. JIT compensation uses an unforgeable in-memory `TransitionReceipt` bound to the admitted transition ID, exact runner ID, slot, and already-completed side effect; it may finish exact ledger bookkeeping or undo that resource after revocation but cannot retry JIT, reserve another slot, run Docker, dispatch work, or refill. Relief cleanup never pulls/creates a container, starts/restarts a backend, dispatches or cancels unrelated work, or expands capacity; emergency cleanup makes no GitHub call. Automatic stale/quarantine/zombie reconciliation is not relief and remains admission-gated. Before automatic pre-start `docker rm -f`, Rust proves immutable managed label, configured slot/name, recorded runner ID, and local ownership; ambiguity skips removal and fails that start attempt.

Slot-ledger parsing is strictly read-only. A corrupt file returns typed `CorruptLedger` without creating a directory, renaming/truncating the file, or continuing reconciliation. Only a higher caller that already owns `AdmissionIssuer` may mint `AdmissionMutation::QuarantineCorruptLedger` immediately before one exact no-replace rename into a canonical quarantine path; after that repair it ends the current reconciliation without reserve/refill, so any later work requires a new iteration and fresh permit. Mint denial leaves the corrupt file untouched and performs no resource side effect. `AuditStateWrite` may still record the denial without carrying a mutation capability.

`ExpiredWhileHealthy` is local control flow: discard the permit and retry `mint_mutation()` once from the latest healthy snapshot. It does not increment `ensure_fail_streak`, call `notify_ensure_failure`, alert, pause the failure ladder, restart a backend, or withhold dead-man credit by itself. Only a remint result of `StaleSince` or `PolicyFailed` enters the failure paths below. Direct one-shot `start` has no background attestor; if its serialized reconciliation outlives the 45-second snapshot, it performs a fresh bounded full runtime admission between mutation boundaries before reminting and never reuses an expired permit.

`AdmissionControl` is an interrupt channel, not a replacement timeout policy. Healthy operations keep their existing deadlines, including the 45-second Docker command timeout, 120-second backend readiness/recovery budget, local readiness budgets, and 75-second queue-monitor budget. Foreground child-process helpers accept both their unchanged deadline and the control token and wait for normal completion, normal timeout, or explicit cancellation. Cancellation terminates the child process group, waits a bounded grace period, then force-kills descendants if needed; it returns a typed cancellation rather than an ordinary timeout. Sleeps and polling waits checkpoint at most once per second. The full-attestation worker alone retains its independent 15-second read-only deadline.

`PolicyFailed` is hard cancellation: the attestor publishes evidence, increments the revocation epoch, emits one cooldown-deduplicated critical alert, and requests foreground cancellation. `serve` checks control at the top of every settle/reconcile/monitor iteration, before and after external operations, and before each runner mutation; the first observed hard cancellation exits `78`, and no worker calls `process::exit`. Child-group cancellation plus one-second checkpoints make the 15-second policy-failure exit bound enforceable without shortening healthy-operation budgets.

A transient refresh may use the last successful snapshot until its 45-second expiry. `StaleSince` initially closes every new `AdmissionMutation`: it rejects every `mint_mutation` but does not cancel already-running read-only monitor/readiness work or an explicit `ReliefCleanup`. The first foreground observation emits dedicated cooldown-deduplicated `containment.attestation.stale`; each observation may also advance `ensure_fail_streak` and `notify_ensure_failure` as supplemental escalation. On the second denied foreground observation or 75 seconds after `StaleSince`, control escalates to transient hard cancellation and `serve` exits `75`. A fresh successful attestation before escalation clears stale state; an expired local permit that remints successfully is not a stale observation. Tests inject clocks, cancellation-aware fake operations, and spies; live evidence records full-check latency and fast-guard p50/p95/p99 at or below 100 ms.

The host installer must first receive successful static and dynamic install verdicts before image acquisition or live mutation, then install and verify policy, non-unit receipt artifacts, and staged unit candidates while holding the transaction lock. Deferred `install-service` independently obtains and consumes `TransactionAdmission` before its only allowed mutation, installing the two units; ordinary admission must then pass every final receipt path before descriptor handoff or start. The shell startup assertion remains defense in depth and consumes only the canonical runtime admission verdict; it does not own a second decision table. Host Docker never invokes VM/Lima recovery, even when a check fails. A failed check returns a typed diagnostic and performs no admission-side Docker, service, config, or live-limit mutation; the sole pre-admission mutation exception is an exact configured Colima/Lima start authorized by a successful static verdict and separate `RecoveryDecision`.

Pre-daemon recovery is not admission. `RecoveryDecision` is the sole backend-start authority. Derived only from the static-validated proposed endpoint during installation or the persisted normalized endpoint at runtime plus the platform, it may execute one bounded start of the exact explicitly configured stopped Colima/Lima backend and is never retried by a generic restart loop. It cannot restart an already-running VM. Canonical host, Docker Desktop, remote, inherited-context, indeterminate, and unsupported-platform endpoints produce `NoRecovery`; a stopped Docker Desktop daemon therefore returns a typed transient dynamic-preflight failure and is never auto-started. After a permitted VM start, full classification and `RuntimeAdmission` are still required before pre-pull or runner reconciliation. Outside a deployment transaction, durable pressure relief remains `systemctl --user stop ezgha.service` followed by ordinary `ezgha stop`; container removal alone is temporary while a supervisor can refill. Transaction-held relief uses the explicit fenced path below.

### Upgrade admission fence

An upgrade must quiesce the currently installed supervisor before image acquisition or any config, helper, receipt, policy, service-manager, Docker-runner, or GitHub-runner mutation. After both read-only install verdicts pass and prior state is snapshotted, it briefly acquires `emergency.lock`, rejects any durable relief latch, and releases that control lock before stopping `ezgha.service` or waiting. It records the old service's active/enabled state, stops only `ezgha.service` when active, and requires `ActiveState=inactive` and old MainPID exit within 30 seconds without holding either lock. It then reacquires `emergency.lock`, rechecks every durable latch, and only on success atomically publishes a mode-`0600` `Preparing` record with schema/generation/nonce, installer PID/start identity, boot ID, canonical intended lock path, config fingerprint, and prior service state. It releases the control lock before attempting the runner fence and never waits for `serve.lock`, service stop, or process exit while holding `emergency.lock`. It opens and retains an exclusive `flock` descriptor within 15 seconds for canonical `state_dir/serve.lock`; after acquisition it transitions the same nonce to `Fenced` with actual device/inode. Any relief latch found before `Preparing`, during fence acquisition, or afterward instead moves the state to `ReliefLatched`, permits no new mutation or later start, and leaves the service inactive. Static-verdict JSON supplies the normalized path so shell never reimplements config resolution. Managed/legacy upgrades must preserve `state_dir`, target/scope, and runner prefix. Release builds remove `EZGHA_SKIP_LOCK`, retaining injection only under `cfg(test)`.

Stopping the supervisor does not cancel a running job. Managed-upgrade containers already proven beneath `actions.slice` may remain while the fence prevents refill. A legacy migration whose containers are not beneath the target aggregate performs a fixed 30-minute drain while holding the lock: it never cancels a busy job, waits for busy containers to finish, removes only idle managed containers/registrations, and requires zero legacy containers before policy mutation. The installer opens a fixed inheritable descriptor, explicitly clears `FD_CLOEXEC`, acquires the exclusive lock, and passes its number only as `--transaction-lock-fd <n>` to the Rust drain primitive and deferred service installer. Before the first mutation, Rust bounds-checks the descriptor, uses `fstat` to match it to a freshly opened canonical lock file, calls nonblocking exclusive `flock` on the inherited descriptor and requires success, then opens a second description and requires its nonblocking exclusive lock to fail with `EWOULDBLOCK`. The first call is idempotent when the shared open-file description already owns the lock and atomically acquires it if the shell failed to do so; it rejects a third-party owner. Rust never unlocks or closes the inherited descriptor, so the parent retains ownership after the child exits. Wrong inode, closed or exec-lost descriptor, or missing second-description contention is rejected before mutation. A drain timeout restores prior bytes before containment/config/helper/image mutation; descriptor-handoff-capable prior state may restore enabled/active state through reserved transfer, while pre-baton state records prior intent but remains disabled and inactive `RecoveryRequired`.

During the transaction, `install-service --defer-start --transaction-lock-fd <n> --unit-candidate-dir <path>` is accepted only with that validated inherited FD plus fresh `TransactionAdmission`; it validates receipt-bound staged bytes, atomically installs and verifies both units, but cannot start them. Final start transfers the existing locked open-file description; it never releases and reacquires the fence. A private Rust `handoff-service --transaction-lock-fd <n>` helper revalidates that inherited descriptor, creates a fresh mode-`0600` Unix socket at canonical `state_dir/handoff.sock` without following or replacing an existing path, and publishes `HandoffReserved` under `emergency.lock`. The reservation binds a random handoff nonce, transaction nonce, boot ID, installer/helper PID-start identities, deadline, protocol version, socket path and identity, candidate-or-rollback mode, expected unit name, and exact binary/config/receipt/unit digests. The generated wrapper always execs hidden `serve --systemd-private`; plain `start` or `serve` has no claim path.

Private mode first performs common read-only systemd provenance with five-second bounded manager queries: environment `INVOCATION_ID` is exactly 32 lowercase hexadecimal characters and equals `systemctl --user show ezgha.service -p InvocationID`; `MainPID == getpid()`; manager `ControlGroup` equals the path obtained by stripping the exact leading `0::` from the unified-v2 entry in `/proc/self/cgroup`, and that normalized path ends in exact `ezgha.service`; `FragmentPath` equals the receipt-bound installed unit path; and `/proc/self/exe`, the CLI config path, and receipt-bound binary/config/receipt/unit modes and digests match installed state. A matching reservation additionally requires the reserved identities and live installer/helper identities before CASing `HandoffReserved -> HandoffClaimed`, then connects to the reserved socket and sends nonce, PID/start identity, and invocation ID. With no transaction, nonterminal handoff, or relief latch, private mode instead acquires `serve.lock` normally, runs the shared terminal-owner resolver, rechecks state, obtains full runtime admission, pre-pulls, and emits `READY=1`. Direct `start` and `serve` use that same post-lock resolver before backend access. A live/malformed terminal owner or any nonterminal, stale, malformed, or mismatched state exits `78` and never falls through.

The handoff helper submits exactly one `systemctl --user start --no-block --job-mode=fail ezgha.service` through a five-second bounded executor while retaining the locked FD and never holding `emergency.lock`. It accepts one connection and requires `SO_PEERCRED` PID/UID plus `/proc/<pid>` start identity, cgroup, executable, config, invocation, nonce, and receipt evidence to equal the matching `HandoffClaimed` state. It sends the already-locked FD over that connection with `SCM_RIGHTS`. The claimant bounds-checks and `fstat`s the received descriptor against a freshly opened canonical lock path, requires idempotent nonblocking exclusive `flock` success on the received shared open-file description and `EWOULDBLOCK` from an independently opened description, then CASes `HandoffClaimed -> HandoffLocked` with the lock identity and sends an `FD_ACCEPTED` acknowledgment. Only after the helper verifies that acknowledgment and state may the installer close its inherited copy. The lock therefore remains continuously held by the same open-file description; no direct process can acquire it between installer and service.

Socket-path cleanup is part of the protocol, not best-effort housekeeping. After validated `FD_ACCEPTED`/`HandoffLocked`, the helper closes its listener, requires `lstat(state_dir/handoff.sock)` to match the reserved socket device/inode/type, unlinks only that identity, fsyncs the parent directory, verifies the path absent, and sends `CLEANUP_COMPLETE` on the accepted connection before reporting handoff success. The claimant must receive that nonce-bound completion and independently prove the reserved path absent before it may publish `ServiceOwned`. If the connection closes or times out first, the claimant may complete the same identity-checked unlink/fsync/absence proof only after proving the reserved helper PID/start identity dead, which also proves its listener closed; a live or unproved helper leaves `RecoveryRequired`. A mismatched replacement is never removed and yields `RecoveryRequired`. Installer success requires the socket path absent. On any failed handoff, matching socket unlink/fsync occurs only after no new peer can connect and any transferred receiver FD has been proven closed.

Submission timeout, signal, transport error, or an unproved rejection is ambiguous. Before any FD transfer, matching `HandoffReserved` becomes unclaimable `SubmissionUnknown`; a fully matching claimed/connected peer may finish the bounded transfer, while a latch, mismatch, failed peer proof, or other state becomes `RecoveryRequired`. No FD is sent after a latch or invalid proof. Failure before a validated ACK closes the socket but retains the installer's fence until state is unclaimable; it then closes the fence, performs bounded job/unit queries and stop without either lock, verifies inactive/MainPID exit, and reacquires `serve.lock` before rollback. After FD transfer but before `ServiceOwned`, failure or relief stops the service without either control lock, waits for its received FD to close, then reacquires and verifies the fence before rollback. A failure to stop, verify closure, or reacquire stays `RecoveryRequired` and starts nothing.

The claimant holding the transferred FD rechecks nonce/digests/latch/deadline and obtains ordinary runtime admission. Only after `CLEANUP_COMPLETE` plus its own absence proof, or the dead-helper fallback proof above, and before `READY=1`, pre-pull, or any runner mutation, it CASes `HandoffLocked -> ServiceOwned` under `emergency.lock`, recording PID/start identity, invocation ID, lock identity, admission evidence, artifact digests, and socket-cleanup evidence. `ServiceOwned` is terminal ownership: its publication proves that no reserved socket path remains, a live matching service survives installer death, and recovery cannot roll it back while its MainPID/invocation/lock evidence remains valid. Installer bookkeeping and READY polling are observational and hold neither lock. If the installer dies before transfer, the service refuses; after transfer, the fully proven claimant may complete ownership, otherwise it records `RecoveryRequired`, closes the FD, and exits without runner mutation.

Queue rejection, peer/transfer/ownership/READY timeout, identity mismatch, or pre-ownership service failure follows the same stop, FD-close verification, fence reacquisition, and rollback sequence. A rollback restart is supported only when the prior receipt and wrapper advertise the same descriptor-handoff protocol version and bind exact prior artifacts; rollback uses an identical reserved socket transfer. Pre-baton or unproven legacy rollback restores byte-exact artifacts, records prior enabled intent, but leaves `ezgha.service` disabled and inactive with `RecoveryRequired`; unreserved rollback start and production lock bypasses are forbidden. Direct `install-service` remains runtime-admission-gated outside a transaction.

Rust-owned `ezgha emergency-stop --confirm` is the only production relief command that bypasses `serve.lock`. Under `emergency.lock` it atomically creates a durable mode-`0600` `ReliefLatched` request with schema, request ID, timestamp, boot ID, optional observed transaction nonce/generation, operator intent, and digest, then releases the lock before waiting. The latch is never automatically deleted, ignored, or weakened because its nonce is absent, stale, mismatched, or malformed. Every direct `start`, `serve`, `install-service`, generated startup gate, transaction phase, and handoff claim checks for any unresolved latch and fails closed before backend/service/runner mutation. A transaction records the request ID/digest, rolls back already-applied bytes under the fence, leaves the service inactive, and never performs candidate or rollback start while latched. Repeated requests are retained as distinct IDs or an atomically auditable list. `--cancel-running` remains separately required for removal; without it, no running container is deleted.

The emergency command may stop `ezgha.service`, but never starts/restarts a backend or service, registers/refills/creates a runner, changes policy/config/receipts, calls GitHub, deletes registrations, or releases slots. With `--cancel-running`, it removes within a 30-second total deadline only a container whose inspection proves all of: label `ezgha=managed`, valid label `ezgha.runner_id`, exact configured slot/prefix name, and runner ID equal to that slot's persisted assignment. Inspect failure, empty/invalid assignment, label/name mismatch, ambiguous ownership, or sibling-host evidence causes a skip. Partial removal returns a distinct incomplete-relief result with removed, skipped, and failed IDs. Docker inspect and approved `docker rm -f` are the only backend operations allowed; offline GitHub registrations remain for the normal reaper after the transaction is gone.

Before and after every bounded transaction operation, before deferred unit install, before publishing/claiming/releasing a handoff, and at least once per second during legacy drain, the current actor checks the durable latch under `emergency.lock`. Latch acknowledgment atomically changes transaction state to `ReliefLatched`; no runner-capable operation begins after that point. A latch arriving before `Preparing`, while the fence is free or busy, during any handoff phase, or immediately before `READY=1` remains durable and blocks start/admission. If candidate service already started, emergency stop stops it; rollback completes only to an inactive state and does not reserve a rollback start.

`emergency-stop` waits at most 35 seconds for acknowledgment and reports incomplete relief after its bounded service-stop attempt on timeout; every request remains durable. Recovery has two tracked forms, neither of which starts anything. Non-emergency `./install.sh --recover-transaction --confirm-transaction <nonce>` exclusively acquires canonical `serve.lock` without holding `emergency.lock`, rejects any unresolved relief request, requires `ezgha.service` inactive, proves no live transaction/service owner by boot ID plus PID start identity, and accepts only matching `Preparing`, `Fenced`, `HandoffReserved`, `HandoffClaimed`, `HandoffLocked`, `SubmissionUnknown`, or `RecoveryRequired`. It completes and verifies the saved byte-exact rollback or proves no mutation, fsyncs a terminal `RecoveredInactive` archive, removes stale handoff socket state, and leaves the service disabled/inactive when prior artifacts are pre-baton. That terminal state permits a later clean installer retry; it does not authorize the old service to start.

Relief clearance uses only `./install.sh --recover-transaction --confirm-request <id>`. After the same fence, inactive-service, dead-owner, and rollback/no-mutation proof, it validates the exact request ID/digest and atomically tombstones only that request under `emergency.lock`, fsyncing the collection. Other unresolved requests do not prevent targeted clearance; they remain durable and block every start, admission, handoff, and non-emergency recovery. When the final request clears a valid transaction snapshot, transaction state archives as `RecoveredInactive`. When no transaction ever existed, final clearance instead fsyncs an explicit `no_transaction_mutation` audit record and creates no recovery archive; the next ordinary start still requires full admission. Busy fence, live owner, active service, wrong identity, malformed/unknown state, or inconsistent snapshot remains fail-closed. Normal `ezgha stop` remains serialized by `serve.lock`; ordinary migration drain never invokes emergency cancellation automatically.

On the fixed Linux profile, the deployment receipt records the final config artifact's path, mode `0600`, SHA-256, and parsed `runner.count`; both the receipt and receipt-validated config must equal `10`. Direct `ezgha start` resolves `requested_count = --count.unwrap_or(config.runner.count)` and rejects with policy status `78` unless `requested_count == receipt.runner_count == config.runner.count == 10`. `--count 10` is accepted but is not an override. Rejection occurs immediately after config load and before lock acquisition, backend selection, containment admission, Docker/GitHub/systemctl invocation, ledger access, or any write. After acquiring the lock, ordinary full runtime admission repeats config identity/count validation to close the pre-lock race. Non-Linux legacy profiles preserve existing configured-count behavior. Runtime attestations and permits bind the persisted count, so no internal caller can expand the profile under a valid permit.

### Backend topology

Host Docker at `unix:///var/run/docker.sock` is an allowed production backend. It is not described as VM-contained. Add a backward-compatible defaulted `[backend].docker_endpoint` field to the deny-unknown config schema. New Linux install/admission requires a recognized explicit value and rejects missing, inherited-only, remote, or ambiguous endpoint state; `ezgha init` persists the normalized recognized endpoint it classified. One endpoint-aware `BackendTopology` classifier, owned by `src/backend.rs` and exposed as structured `ezgha topology --json`, combines that persisted endpoint with Docker daemon metadata: the canonical host socket plus a same-kernel Linux daemon is `HostDocker`; a recognized explicit Colima, Lima, or Docker Desktop socket plus VM daemon metadata is `VmDocker`; contradictory metadata, remote endpoints, and unknown sockets are `Indeterminate`. `Platform.daemon_in_vm` is only one metadata input and is never the classifier by itself. Shell gates consume the structured Rust result and do not duplicate the endpoint decision table. A Linux install/start gate rejects `Indeterminate`, while existing Mac and pre-existing runtime paths preserve their non-fatal warning/no-op behavior.

Legacy Mac configs without `[backend].docker_endpoint` use a Darwin-only compatibility resolver until their next `ezgha init` or `install-service`. It considers, in order, a nonempty explicit override, the installed launchd plist endpoint, the active Docker context, and recognized Colima/Lima/Docker Desktop sockets; it accepts only one normalized consistent endpoint and returns `Indeterminate` on disagreement, unknown transport, or no evidence. The resolved endpoint is persisted atomically during `init`/`install-service`; after persistence the config value overrides ambient state. A uniquely proven Colima/Lima endpoint retains the existing bounded recovery, while Docker Desktop, ambiguity, or missing evidence produces `NoRecovery` and never invokes Colima/Lima. This compatibility resolver is forbidden on Linux.

The generated unit always emits the config endpoint, including `unix:///var/run/docker.sock`, and clears inherited `DOCKER_CONTEXT`; every Rust Docker subprocess uses that same value and removes `DOCKER_CONTEXT`. The generated Linux service has no unconditional `lima-vm@colima.service` dependency, and backend recovery must never start Lima for `HostDocker`; a failed HostDocker permit is a hard no-op, never a VM recovery request. Only explicit Colima/Lima endpoints may use the existing bounded recovery path; Docker Desktop is classifiable when reachable but is non-recoverable by this implementation when stopped. An inactive legacy Lima unit may be disabled after the backend endpoint is proven; no VM disk is deleted.

For the `HostDocker` branch, the service may start only after the containment preflight establishes all of the following; `VmDocker` reports this host profile as not applicable and proceeds only through its separate VM gate:

1. The configured endpoint is exactly the canonical host socket, not a Colima/Lima or remote endpoint.
2. Docker reports cgroup v2 with the `systemd` cgroup driver.
3. The configured cgroup parent is `actions.slice`.
4. `/sys/fs/cgroup/actions.slice` exists under the system manager.
5. Its memory, swap, CPU, and task limits exactly match the declared fixed profile.
6. `actions.slice` is enrolled for memory-pressure handling.
7. The deploy owner's `user@UID.service` is not actively monitored for memory pressure or swap, no ancestor is enrolled with `kill` for either policy, and the broad service reports `ManagedOOMPreference=none` plus `OOMScoreAdjust=0` rather than a subtree-wide exemption.
8. The tracked finite `agents.slice` is effective and no unlimited local override is active.
9. Neither the installed `ezgha.service.d/10-oomd-omit.conf` nor its effective `ManagedOOMPreference=omit`/`OOMScoreAdjust=-1000` remains.
10. Unprivileged bounded `oomctl` queries work and list the intended workload roots, including effective user-manager `agents.slice` and `automation.slice` enrollment.
11. The host has at least 62 GiB RAM and 32 logical CPUs; this fixed hardware profile is rejected on a smaller machine instead of silently consuming its reserve.

Any failed invariant blocks new runner admission and emits a diagnostic naming the mismatched property. It does not restart Docker, Colima, the desktop, or the physical host.

## Components and Ownership

### Repository policy

`CLAUDE.md` owns the full crash-containment tenet. `AGENTS.md` contains a concise pointer because both files exist independently. `README.md` describes the user-visible topology accurately and stops claiming that Jeff-Ubuntu runners are inside Colima when host Docker is active. `/up` writes a short receipt under `roadmap/`.

### System policy artifacts

`systemd/host/actions.slice` owns the host runner aggregate. A separate top-level `systemd/host/ezghaproof.slice` contains `ezghaproof-hardlimit.slice` and `ezghaproof-oomd.slice`, so a synthetic proof is never a descendant of the monitored production runner root. Fixed root-owned `ezgha-hard-limit-proof.service` and `ezgha-oomd-proof.service` units are the only proof launchers; they use a repository-owned Python-stdlib allocator, have no Docker socket or credentials, and cannot accept an arbitrary command. Hard-limit mode deliberately exceeds its child `MemoryMax`. Oomd mode allocates and continuously touches an anonymous working set above its child `MemoryHigh` but below `MemoryMax` for 75 seconds. Because systemd exposes pressure duration only as the global `DefaultMemoryPressureDurationSec`, the harness reads and records the effective merged oomd configuration and accepts only an unambiguous integral whole-second value in `1..=45`. Missing, malformed, zero, negative, fractional, unit-suffixed, or larger values fail. It requires the child's exact `memory.pressure` `full avg10` value to stay at least 20% continuously for that duration before accepting a correctly named journal victim. Allocator exit without both the continuous measured interval and victim never passes. The proof harness uses privileged `systemctl` only with those literal unit names and never permits `systemd-run` or a shell. `systemd/host/-.slice.d/90-ezgha-oomd-boundary.conf` and `systemd/host/user@.service.d/90-ezgha-oomd-boundary.conf` own the broad vendor-policy overrides. Existing `systemd/agents.slice` and `systemd/automation.slice` own user workload limits.

The workload-only oomd policy is intentional: Ubuntu's vendor `user@UID.service` kill behavior is the broad crash blast radius being removed, not a useful workload safeguard. All desktop ancestors are explicitly `auto`, and the user service is neutral rather than `avoid`/`omit` or negatively scored; `oomctl` must show only the finite workload roots (and an active proof root) as kill-capable roots. The proof must demonstrate that a workload victim does not terminate the user manager, Warp, or unrelated desktop processes.

### Installation

`scripts/host/install-crash-containment.sh` installs and verifies system-scope artifacts with explicit privilege escalation. It supports a read-only capability/current-use `--preflight`, a read-only installed-state `--check`, and an applying `--install` mode. `install.sh` invokes the installer on systemd Linux hosts and fails rather than claiming success when the configured host-Docker fleet lacks the required system controls.

After an allowed local candidate build, installation has two read-only phases. Static preflight first validates a mode-`0600` proposed config with an explicit endpoint, hardware, privilege, deployment state, and rollback storage without requiring a reachable VM daemon. The state classifier does not treat a config file alone as an upgrade:

- `Bootstrap` requires no receipt/helper set, generated user units, active/enabled runner service, managed-prefix container, or registered-prefix runner. The active config may be absent or byte-identical to the proposed config and is staged data, not deployment evidence; any partial runtime/policy artifact is `Mixed`.
- `ManagedUpgrade` requires a complete receipt, every receipt-listed artifact, both generated user units, the active config, and all expected effective cgroups. It enforces the current-use margins.
- `LegacyMigration` requires a recognized complete pre-receipt service/config/runtime signature. It is never adopted as fresh and must pass the upgrade admission fence plus bounded legacy-container drain.
- `Mixed` covers partial receipts/helpers/units/cgroups, an unrecognized differing active config, orphaned registered-prefix runners, or any other incomplete combination and fails before image, runner, policy, config, or service mutation.

A receipt whose unit entries exist only in the receipt-bound candidate directory is an allowed transitional state solely inside the running transaction while the validated exclusive lock is held. It is never a persisted deployment classification. After process interruption or without that exact lock/candidate proof it classifies `Mixed`, ordinary admission fails, and no service may start; rollback removes or restores the receipt and all listed artifacts.

A terminal `RecoveredInactive` archive is not itself deployment evidence and is not automatically `Mixed`. The classifier first verifies its transaction nonce, exact saved snapshot digest, completed byte-exact rollback, absent handoff socket, inactive service, and disabled state when restored artifacts are pre-baton, then classifies the restored artifacts normally as `Bootstrap`, `ManagedUpgrade`, or `LegacyMigration`. Any archive mismatch remains `Mixed`. A clean retry consumes/archives the recovery marker only after publishing its own new `Preparing` nonce.

This supports the documented two-pass bootstrap: the first `./install.sh` performs only `cargo build --locked --release` in the checkout and requests `target/release/ezgha init`; `init` may read endpoint/daemon metadata but cannot start/recover a backend or build/pull/run an image, and writes only an explicit-endpoint starter config. The second `./install.sh` treats that config-only state as staged `Bootstrap`. It does not replace the installed binary before preflight. Current `install.sh` config-presence auto-start/restart is removed; only the containment transaction may atomically promote the candidate binary and call `install-service`. Only a recognized explicit Colima/Lima endpoint may authorize starting that exact configured VM between static and dynamic phases. Dynamic preflight then proves daemon topology, Docker cgroup v2 with the `systemd` driver, and unprivileged `oomctl`. Both phases precede admission quiescence, image acquisition/build/pull, and every installed/live mutation. Canonical host, Docker Desktop, remote, inherited-only, indeterminate, and unsupported-platform endpoints never recover a VM; a failed phase exits without image work or changing the installed binary, generated service, config, helpers, or live limits. The supported portable target is a fresh systemd Ubuntu host meeting the declared 62-GiB/32-CPU profile; scaled profiles are a separate reviewed design, not an implicit fallback.

Installation removes the known `agents.slice.d/99-local-unlimited.conf` drift file and the live `~/.config/systemd/user/ezgha.service.d/10-oomd-omit.conf`, disables and removes the obsolete `psi-oom-watcher` service/timer plus its installed stable script, and reloads the appropriate managers without restarting the desktop or physical host. It verifies the exemption's unit configuration is gone before restart, then verifies the new MainPID's `/proc/<pid>/oom_score_adj` and effective properties after restart. Before lowering an active hard limit, it records `memory.current` and `memory.stat` for the actual system and user cgroups, but uses the enforceable `memory.current` value without discounting file cache to prove use fits below the proposed `MemoryHigh` with a workload-specific activation margin: 1 GiB for actions, 1 GiB for agents, and 512 MiB for automation; otherwise it leaves the live policy unchanged and fails. The actions margin permits the declared ten-container maximum of about 24.4 GiB below `MemoryHigh=26G`. The single `install.sh` transaction snapshots the installed `ezgha` binary bytes/path/mode/digest, live config, all replaced system/user policy and helper files, both generated `ezgha.service` and companion `ezgha-alert@.service`, the versioned deployment receipt and every receipt-listed artifact, the legacy watcher service unit, timer unit, and installed stable script, the live exemption, and the applicable prior active/enabled state for every affected unit. After both read-only verdicts it performs the upgrade admission fence, then atomically installs the candidate binary, mode-`0600` proposed config, and helper/policy set. Any quiescence, binary, config, containment, assertion, service regeneration, restart, or post-start verification failure restores every file byte-for-byte and reloads both managers. Receipt-proven descriptor-handoff state may restore enabled/active state through reserved transfer; pre-baton state records prior intent but remains disabled/inactive `RecoveryRequired` until verified recovery and a clean installer retry. It verifies the effective restored state before returning failure, installs only persistent tracked units, never creates overriding `systemctl set-property` drop-ins, and never deletes VM data.
The installer also publishes a versioned deployment receipt atomically with final `config.toml`, the candidate binary, helper set, and staged generated-unit candidates. The receipt records the deployed source commit SHA, explicit `startup_handoff_protocol = 1`, and, for config, the binary, every helper, `ezgha.service`, and `ezgha-alert@.service`, the artifact role, candidate/source path, final installed path, exact mode, and SHA-256. Protocol 1 means the bound wrapper uses the exact private entrypoint and the bound binary implements peer-proven Unix-socket `SCM_RIGHTS` transfer plus ACK; absent, unknown, or inconsistent protocol is pre-baton for rollback. The config entry additionally records parsed `runner.count`; fixed Linux requires mode `0600` and count `10`. Config, files, and unit candidates are staged, mode/digest/count-checked, flushed, and renamed before the receipt is renamed into place; a receipt that does not describe this complete deployment set is invalid. The transaction-scoped permit validates final config and installed binary/helpers plus staged unit candidates, allowing deferred service installation without circularly requiring new units to be installed first. After deferred installation, ordinary admission validates every final receipt path, mode, digest, parsed count, and supported startup protocol before descriptor handoff. Runtime commands execute the receipt-validated config and installed immutable binary/helpers and never use a mutable checkout as deployment evidence. The receipt and every artifact it describes are one rollback unit: bootstrap rollback removes them together, upgrade rollback restores their prior bytes together, and either path rejects partial state.

### Startup preflight

`scripts/host/assert-crash-containment.sh` is a read-only executable installed under `~/.local/libexec/ezgha/`. The generated `ezgha.service` uses a tracked wrapper as `ExecStart`, passing exact receipt-bound binary/config paths. It runs canonical `containment-check --admission --json` plus the defense-in-depth assertion, propagates `75`/`78`, and on success executes exactly `exec "$binary" --config "$config" serve --systemd-private`. That hidden flag is accepted only with the systemd provenance proof above: a matching reservation enters descriptor handoff, while absence of transaction/handoff/latch enters ordinary serialized systemd startup. Plain direct `start`/`serve` cannot claim a transaction handoff. The assertion performs three Docker queries over at most 15 seconds; preserved restart/start-limit and `TimeoutStartSec=150` settings bound startup. Backend classification uses configured endpoint plus daemon metadata. Every final receipt path/mode/digest must match installed state, not checkout.

Service regeneration preserves failure alerting. The main unit retains `OnFailure=ezgha-alert@%N.service` plus a fail-open `ExecStopPost` that invokes the exact installed binary/config with `systemd-alert-hook --source exec-stop-post`. The companion `ezgha-alert@.service` remains a 30-second `Type=oneshot` invoking the same binary/config with `--source on-failure`; both units inherit the persisted endpoint and cleared Docker context, and both are included in transactional rollback tests.

The assertion returns distinct statuses for a proven policy violation and a transient inability to query Docker. The generated service permits recovery from a short transient failure within its existing bounded start window, while a proven policy violation remains fail-closed and clearly logged. This is normal service recovery, not a second repair daemon and never mutates containment policy.

### Verification

`docs/verify-exit-criteria.sh` verifies effective values rather than file presence. Host-Docker and Colima gates are mutually exclusive: the host path checks the system `actions.slice`, the `systemd` Docker cgroup driver, actual cgroup ancestry for every managed container, user workload caps, the `user@` OOM policy and all desktop ancestors, effective `oomctl` roots, and the absence of unlimited/runtime overrides and `ezgha.service` OOM exemptions. It also validates the versioned deployment receipt against installed binary, helper, and generated-unit paths, modes, and digests. It does not include an inactive Lima slice in host-Docker budget arithmetic or require a live guest. `doctor-runner` reports the same topology without mutating it.

## Data Flow

1. `install.sh` may build the exact local candidate binary locally (the build cannot invoke Docker), classifies `Bootstrap`, `ManagedUpgrade`, `LegacyMigration`, or `Mixed`, then requires a successful static install verdict. For an explicit recognized Colima/Lima endpoint on a supported platform only, it may start that exact VM, then requires the dynamic Docker-backed install verdict. Host Docker and Docker Desktop never recover a VM. Neither verdict is an admission capability.
2. After both verdicts and a read-only transaction snapshot, an upgrade stops the old supervisor, verifies it inactive, and holds canonical `serve.lock`; legacy containers drain without cancelling busy jobs. Both phases and admission quiescence must pass before image acquisition or any other config/helper/policy/runner mutation.
3. The host containment installer copies tracked units and the root-owned proof allocator to their system and user destinations, atomically retires the old watcher and both source/live exemption state, reloads managers, and checks effective properties including `oomctl` roots.
4. After containment, deferred installation validates the inherited lock/staged receipt, consumes `TransactionAdmission`, and installs units without starting. Ordinary admission validates final artifacts. While fenced, the Rust handoff helper reserves and queues exact private systemd mode, verifies its claimed MainPID, and passes the continuously locked open-file description through `SCM_RIGHTS`; the claimant validates and sends `FD_ACCEPTED` before any installer copy closes, then proves listener closure and socket cleanup before recording terminal `ServiceOwned`, `READY=1`, or runner work. With no transaction/handoff/latch, the same private wrapper takes ordinary serialized systemd boot/restart. Long-lived serve remints one-use admission before every `AdmissionMutation`; only exact `ReliefCleanup` bypasses healthy admission. Pre-ownership failure or relief latch restores inactive prior state unless compatible descriptor-handoff rollback succeeds; pre-baton rollback remains disabled/inactive, and live valid `ServiceOwned` is not rolled back after installer death.
5. The endpoint-aware topology enum controls budgeting: `HostDocker` ignores VM-only guest-reserve fields and delegates the aggregate ceiling to system `actions.slice`; `VmDocker` retains the existing guest reserve and VM envelope; `Indeterminate` is rejected by new Linux install/start gates while preserving the bounded legacy-Mac resolver.
6. `ezgha` creates each runner with `--cgroup-parent=actions.slice` plus per-container memory, CPU, PID, swap, and security limits.
7. The system manager accounts every runner beneath the finite aggregate.
8. Under ordinary pressure, `MemoryHigh` throttles and reclaims workload memory.
9. Under sustained pressure, `systemd-oomd` may kill an eligible descendant of that monitored workload root; at the hard boundary, the kernel enforces `MemoryMax` within the limited cgroup.
10. Separate hard-limit, oomd-selection, ten-worker execution, and live desktop-survival proofs record what actually happened instead of inferring behavior from configuration.

## Residual Risks

- The approximately 8.5-GiB remainder (`62.48 - 54 = 8.48 GiB`) is not reserved memory. Docker, the kernel, desktop applications, and other system services remain outside the three hard caps and can still trigger global kernel OOM.
- Independent oomd monitoring roots do not provide a global runner-before-agent order. Each root selects only among its own eligible descendants.
- `MemorySwapMax=0` avoids runner swap thrash but reduces the time available for PSI-based oomd action during an abrupt allocation burst; `MemoryMax` is the final boundary.
- `CPUQuota=2000%` equals the ten configured 2-CPU container maxima and reserves 12 of 32 logical CPUs for the host; the capacity proof still checks real ten-job execution.
- Portable block-device bandwidth caps require a measured device baseline. `IOWeight=25` gives runner I/O lower relative priority where the controller is available; the verifier reports unavailable controllers rather than asserting protection.

## Failure Handling

| Failure | Required behavior |
|---|---|
| Host `actions.slice` missing or infinite | The main startup wrapper exits `78`; no new runners start and systemd does not restart the policy violation. |
| Static or dynamic preflight fails, or scoped admission is missing/expired/receipt-invalid | Preflight leaves live mutation unchanged except one exact statically authorized Colima/Lima start. Failed full admission blocks direct start, serve pre-pull, direct install, and direct canary dispatch; stale/revoked mint blocks the next automatic Docker/GitHub/ledger/quarantine/workspace/control side effect. `ExpiredWhileHealthy` remints without failure-ladder side effects; exact `ReliefCleanup` remains available. |
| Linux direct count differs from receipt-backed config or fixed value 10 | Exit `78` immediately after config load with zero lock, backend, containment, Docker, GitHub, ledger, or write operation. Full post-lock admission repeats the validation before reconciliation. |
| Background attestation proves policy drift | Increment revocation epoch, block the next mutation, alert once, cancel foreground child groups without changing healthy timeout policy, and exit `78` within 15 seconds. |
| Background attestation is transiently unavailable past snapshot TTL | Enter soft `StaleSince`, deny new mutation and alert on first observation; after two denials or 75 seconds hard-cancel and exit `75`. A fresh pass before escalation clears stale. |
| Handoff queue, peer, FD transfer, ownership, or READY fails | Before `ServiceOwned`, record `RecoveryRequired`, stop/wait inactive without locks, verify the received FD closed, and reacquire before rollback. A live valid `ServiceOwned` survives installer death. Compatible rollback repeats descriptor transfer; pre-baton rollback restores bytes but remains disabled/inactive and has an exact non-emergency recovery path. |
| Confirmed emergency stop at any time | Persist every `ReliefLatched` request even without matching transaction state. Direct/startup/handoff admission fails closed; an active transaction rolls back and leaves service inactive. Exact targeted clearance removes one request while others continue blocking; the final clearance never starts anything. Running containers require `--cancel-running` plus exact ownership proof. |
| Bootstrap/upgrade state is mixed, or upgrade quiescence fails | Installation fails before image or containment/config/helper mutation. If the old service was stopped, compatible state may restore through descriptor handoff; pre-baton state remains disabled/inactive and recoverable for installer retry. Busy jobs are never cancelled. |
| Docker query is temporarily unavailable | The assertion retries for at most 15 seconds and returns `75`; normal service restart retries at 30-second intervals and the existing five-start/300-second limiter stops a persistent outage. After fixing the prerequisite, the tracked runbook requires a passing `ezgha containment-check --admission --json`, then `systemctl --user reset-failed ezgha.service` and `systemctl --user start ezgha.service`; no monitor resets or loops the limiter. |
| One runner exceeds its limit | The cgroup-local OOM is contained to that runner scope. Whole-container exit is claimed only when effective `memory.oom.group` or observed exit behavior proves it. |
| Runner aggregate exceeds `MemoryHigh` | Reclaim/throttle occurs inside `actions.slice`. |
| Sustained runner pressure | An eligible runner descendant is selected within `actions.slice`; no cross-root runner-before-agent order is claimed. |
| Agent aggregate exceeds its envelope | Pressure handling remains within `agents.slice`; desktop survival is a live integration result, not a static guarantee. |
| `systemd-oomd` inactive | Hard cgroup limits still contain workloads; verifier reports degraded defense in depth. |
| Host Docker selected while Lima unit is enabled | Verification fails with topology drift; installation may disable an already inactive Lima unit but never stops a live VM automatically. |
| System privilege, topology, driver, hardware, or `oomctl` preflight unavailable | Installation fails before mutation with the exact unmet prerequisite; it does not deploy a falsely green user-only approximation or restart the fleet. |
| Post-mutation verification or gated restart fails | Restore final config, system/user policy, binary/helper files, receipt, units, and retired watcher/exemption files. Restore enabled/active state only through receipt-proven descriptor handoff; otherwise verify byte-exact disabled/inactive `RecoveryRequired` rollback and its tracked recovery path. |
| Pressure proof harms the desktop or host | Test fails immediately and blocks deployment; no threshold is accepted from synthetic unit tests alone. |

## Testing Strategy

### Static and fixture tests

- Validate systemd unit syntax with `systemd-analyze verify`.
- Test the installer in a temporary root with stubbed `systemctl`, `sudo`, Docker endpoint, and cgroup files.
- Cover deployment-state classification, config-only bootstrap, mixed/partial artifacts, active-service quiescence, busy-job-preserving legacy drain, state/target/scope/prefix drift, inherited-FD proof and rejection branches, staged/final receipt validation including config count, deferred transaction admission type isolation, byte-exact config/unit/artifact rollback, host/VM/contradictory topology, Mac migration, cgroup/oomd policy drift, broad-session exemption rejection, agent enrollment, and all six runner labels plus exact four-label fleet collision proof.
- Cover fixed-Linux count zero-mutation rejection; a serialized ten-runner refill over 300 seconds; one-use permits before every automatic Docker/GitHub/ledger/quarantine/workspace/reaper/canary side effect; independent permits for every retry and JIT conflict DELETE/retry; revocation between actions; exact compensation-only cleanup; direct-start re-attestation; unchanged healthy budgets; child-group cancellation; and explicit stop/SIGTERM/emergency cleanup while admission fails.
- Cover reservation/claim/FD-transfer/lock/ownership for candidate and compatible rollback; exact hidden wrapper; ordinary restart; nonterminal fallthrough rejection; invocation, manager/proc, socket, `SO_PEERCRED`, executable/config/receipt/unit proof; `SCM_RIGHTS` continuity and independent-lock contention; installer/helper/claimant death before/after transfer; emergency at every boundary; ambiguous submission; stop/received-FD closure/reacquisition; pre-baton disabled recovery; repeated requests cleared independently; no locks while waiting; and crash/recovery at every state.
- Cover legacy Mac config parsing, unique Colima/Lima endpoint migration and recovery, reachable/stopped Docker Desktop with zero Colima calls, ambiguous legacy endpoint `NoRecovery`, persisted-config precedence over ambient state, and Linux missing-endpoint rejection.
- Verify generated service/drop-in installation and removal behavior without touching the live fleet.
- Verify README, `CLAUDE.md`, and `AGENTS.md` contain one canonical semantic rule plus pointers rather than duplicated policy bodies.

### Integration checks

- Run Rust tests and focused shell tests.
- Run the containment assertion against the live host before restarting `ezgha`.
- After deployment, dispatch `capacity-proof.yml` with the unique four-label capacity selector `[self-hosted, self-hosted-mikey, ezgha, ez-runner-c]` and prove ten managed containers simultaneously run `Runner.Worker` under `actions.slice`; all six labels remain advertised by each runner, while idle slot presence or a workflow scheduled to a different fleet is not capacity proof.
- Record actual cgroup paths and `memory.oom.group` for all managed containers.
- Run separately labeled checks: a bounded `ezghaproof-hardlimit.slice` `MemoryMax` test, an isolated `ezghaproof-oomd.slice` `systemd-oomd` PSI/victim-selection test, an exact-run-ID ten-worker execution proof using direct `docker top`, and a host/desktop survival observation. None substitutes for another. Both synthetic tests use only fixed tracked system services and verify production runner scopes are outside the proof subtree before allocation.
- Record host and user-manager boot IDs/PIDs before and after live proofs. The desktop, Warp, user manager, Docker daemon, and unrelated runner slots must remain alive.
- Run `doctor-runner` and the exit-criteria harness. Fleet capacity remains ten Linux runners; full job execution proof follows the repository's existing capacity gate.

## Acceptance Criteria

1. Host Docker is explicitly reported and accepted as the Linux production backend.
2. Ten Linux runners remain configured with six advertised labels (`self-hosted`, `self-hosted-mikey`, `ezgha`, `ez-runner-c`, `Linux`, `X64`); every live runner container is under system `/actions.slice`, and a registration-scope query proves the capacity workflow's four-label selector `[self-hosted, self-hosted-mikey, ezgha, ez-runner-c]` matches exactly those ten runner IDs/names and no collision.
3. Docker uses cgroup v2 with the `systemd` driver; effective runner aggregate limits are `MemoryHigh=26G`, `MemoryMax=28G`, `MemorySwapMax=0`, `TasksMax=6000`, `CPUQuota=2000%`, and `IOWeight=25` where the I/O controller is available.
4. Effective agent aggregate limits are `MemoryHigh=18G`, `MemoryMax=20G`, `MemorySwapMax=2G`, and `TasksMax=8192`; activation proves the unadjusted `memory.current` value is below `MemoryHigh` with the 1-GiB margin while recording `memory.stat` for diagnosis, no unlimited override or launcher opt-out remains, and every detected supported agent PID is beneath the slice.
5. Effective automation aggregate limits remain `MemoryHigh=4G`, `MemoryMax=6G`, `MemorySwapMax=1G`, and `TasksMax=4096`.
6. Gate 8 proves the finite host aggregate is exactly `28G + 20G + 6G = 54G`, requires at least 62 GiB physical RAM, reports about 8.48 GiB (about 8.5 GiB) remainder on the measured 62.48-GiB host, and excludes inactive Lima capacity from host-Docker arithmetic.
7. Workload aggregates are enrolled for memory-pressure handling at 50%; `-.slice`, `user.slice`, the deploy owner's `user-UID.slice`, `user@UID.service`, and all desktop ancestors report both `ManagedOOMMemoryPressure=auto` and `ManagedOOMSwap=auto`; `user@UID.service` is neutral with `ManagedOOMPreference=none` and `OOMScoreAdjust=0`; unprivileged `oomctl` lists only the finite workload roots (plus the active proof root); and `ezgha.service` has neither `ManagedOOMPreference=omit` nor `OOMScoreAdjust=-1000`.
8. The three containment-check modes and internal gate share one Rust implementation. Static/dynamic verdicts and quiescence precede mutation; private `TransactionAdmission` installs/reloads only staged artifacts. One exact explicit Colima/Lima start is the only backend recovery. Every automatic Docker/GitHub/ledger/quarantine/workspace/reaper/canary side effect consumes a one-use `AdmissionMutation`; narrow proven downward `ReliefCleanup` remains available. Transaction scope cannot reach runners and no permit is serialized.
9. `ezgha.service` refuses startup on a proven host-Docker containment violation and can recover from a transient Docker query failure without exhausting its start limit. The versioned deployment receipt is atomically installed, validates the deployed SHA/source plus final config path/mode/digest/parsed count and installed binary/helper/generated-unit paths, modes, and digests, and rolls back with every listed artifact.
10. Separate executable evidence proves child hard-limit containment, isolated oomd descendant selection, ten simultaneous `Runner.Worker` processes tied by in-container run markers to one successful capacity workflow run selected by the unique four-label selector, and host/desktop survival; claims are limited to the proof that produced them.
11. The shared endpoint-aware topology classifier makes Host-Docker runtime and doctor paths ignore VM-only `vm_total_mb` and guest-reserve arithmetic, preserves those checks for explicit VM topology, migrates a uniquely resolved legacy Mac endpoint without guessing, and lets new Linux install/start gates reject contradictory or unknown topology.
12. Config-only bootstrap is distinct from managed and legacy upgrade state; the current supervisor is inactive and canonical `serve.lock` is held before mutation, legacy busy jobs drain without cancellation, and pre-baton rollback restores bytes while staying disabled/inactive until tracked recovery enables a clean retry.
13. No new watchdog, host restart authority, VM fallback, or runner-count reduction is introduced.
14. All controls, installation steps, verification logic, bootstrap/recovery instructions, and the crash-containment tenet are git-tracked and reproducible on a fresh Ubuntu host.
15. The fixed Linux profile rejects any direct-start count that is not receipt/config-backed `10` before lock/backend/admission/external calls or writes, and repeats identity/count validation after locking before reconciliation.
16. Full attestation is single-flight, read-only, scheduled within 20 seconds of completion, bounded to 15 seconds, and outside foreground budgets. Every `AdmissionMutation`, including retries and automatic cleanup, consumes one fresh permit; healthy generations support a serialized ten-runner batch beyond 300 seconds, while revocation blocks the next side effect. Existing healthy timeouts and explicit cancellation bounds remain unchanged.
17. Final candidate and compatible rollback starts transfer the continuously locked open-file description over a peer-proven Unix socket using `SCM_RIGHTS`, then transition `HandoffReserved -> HandoffClaimed -> HandoffLocked -> ServiceOwned`. No installer copy closes before validated `FD_ACCEPTED`; listener closure plus identity-checked socket cleanup and absence are proven before ownership, READY, or runner work. Ordinary boot opens the lock normally only with no active state. Pre-baton rollback remains disabled/inactive but has exact tracked recovery. Repeated relief requests clear independently and verified recovery never starts anything.

## Implementation Preconditions

- The deploy owner must have non-interactive or interactive `sudo` authority to install system units under `/etc/systemd/system`; user-only installation cannot enforce Docker's system cgroup hierarchy.
- The live deployment must follow the repository's single-writer checks before restarting `ezgha.service` or running the live harness.
- The bounded pressure proof requires privilege to start the two fixed tracked proof services by literal name; arbitrary transient units and commands are prohibited. The repository supplies the fixed bounded allocator rather than depending on `stress-ng` behavior.
- Live activation requires current usage to fit below each proposed `MemoryHigh` with the declared workload-specific margin: 1 GiB for actions, 1 GiB for agents, and 512 MiB for automation; otherwise deployment stops before installing or reloading units.
