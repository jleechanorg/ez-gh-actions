# Next Oops must reboot instead of hanging for hours.
# Jeff-Ubuntu 2026-08-26: CFS/nohz NX panic at ~02:41, host hung until 08:55
# because kernel.panic=0 (never reboot on panic) and kdump is unarmed.
kernel.panic = 10
kernel.panic_on_oops = 1
