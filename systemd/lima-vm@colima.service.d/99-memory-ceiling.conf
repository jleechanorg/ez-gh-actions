# Direct QEMU containment. Lima uses an escaped transient application slice on
# this host, so the service cgroup is the stable enforcement point.
[Service]
MemoryHigh=34G
MemoryMax=38G
MemorySwapMax=2G
TasksMax=4096
MemoryAccounting=yes
