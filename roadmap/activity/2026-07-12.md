# Activity - 2026-07-12

- **/nextsteps + code-standards review:** pulled the 5-PR merge-checklist sidekick verdict (all PASS on substance, Lane F commit-prefix FAIL x5), opened PR #62 (deadline-clamping fork from the closed PR #56 scope fraud), and shipped a repo-local /code-standards command that layers 10 repo gates on user-scope ZFC + ponytail. The Mac fleet regressed 6->3 during this window; `yz6b` guard fired as designed. Missed 4 open PRs (#52 #63 #64 #65) on the first /nextsteps pre-step (filed `j2ym`). Closed bead `sa1t` continues to deliver; user-scope half of PR #54 installed across both machines.

## Ten-runner host-availability program

- Root cause was refined from "runner count" to aggregate capacity collapse:
  a roughly 48 GiB runner VM plus unbounded agent/factory/browser workloads
  exhausted a 62 GiB host. Container/VM security boundaries did not provide a
  host-wide availability budget.
- Operator selected 10 Linux runners. Created umbrella bead
  `ez-gh-actions-e0z0` and soak bead `ez-gh-actions-e0z0.1`.
- Opened [issue #71](https://github.com/jleechanorg/ez-gh-actions/issues/71)
  for the host-first capacity envelope,
  [issue #72](https://github.com/jleechanorg/ez-gh-actions/issues/72) for
  mandatory agent/MCP containment, and
  [issue #73](https://github.com/jleechanorg/ez-gh-actions/issues/73) for
  72-hour ten-runner proof.
- Closed stale merge wrappers `ez-gh-actions-c5go` and
  `ez-gh-actions-pcss`; PRs #54 and #55 are merged. Closed delivered
  implementation bead `ez-gh-actions-yz6b` and capacity-decision bead
  `ez-gh-actions-qyzo`; the new program owns live deployment and proof.
- Dependency order: PR #52 pressure admission -> VM resize and aggregate
  runner cgroup -> mandatory agent containment -> staged shedding -> bounded
  real-job pressure proof. The rejected 72-hour soak and issue #73 are closed.

## 19:35 global-OOM and watchdog reboot

- Kernel evidence: swap had 228 KiB free and global OOM killed the Linux
  runner QEMU at 37.6 GiB anonymous RSS. `ezgha.service` recorded a 36.7 GiB
  peak; load rose above 300 during reclaim.
- Watchdog evidence: load crossed the 96 threshold, the repair policy failed
  to demonstrate falling QEMU RSS/pressure within its window, and watchdog
  initiated shutdown at 19:37. This was not a kernel panic.
- Isolation finding: per-container limits and the Colima VM isolated workloads
  but did not enforce an aggregate host budget. The QEMU parent cgroup had
  unlimited `MemoryHigh`, `MemoryMax`, and `MemorySwapMax`.
- P0 order: deployment alignment, aggregate runner cap, host-pressure
  admission, mandatory agent/MCP enrollment, fail-closed stale reconciliation,
  staged QEMU shedding, then watchdog pressure proof.
- Mac restoration: live ProjectDirs config was intentionally reduced to three
  runners because a 16 GiB VM could not fund six. Restore six only with a
  larger VM, explicit per-runner limits, durable token auth, and local
  `Runner.Worker` proof for all six slots.
- **Remediation & Active Health Verification (Antigravity):**
  - Compiled and deployed the latest binary (`0e32169` at HEAD) to `/home/jleechan/.cargo/bin/ezgha`, resolving the Gate 0 compile mismatch.
  - Restarted `ezgha.service` to apply the merged memory budget clamping. The service is actively running and clamping memory limit to `3010 MB` and CPUs to `1.5` per runner.
  - Stopped the redundant 12GB Colima VM (`colima stop`), reclaiming 12GB of RAM on the host (free available memory increased to 42GB).
  - Verified that all gates (0, 1, 2, 3, 4, 7, 10) are passing, including executing a fresh nonce-tracked canary run in 59s.

