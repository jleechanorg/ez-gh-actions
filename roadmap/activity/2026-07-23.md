# Activity — 2026-07-23

## AO ecosystem mission v2 closure + live Go AO cutover test

- Closed mission `jleechan-1eds` (AO ecosystem mission v2 — 4h autonomy, all 6 PRs merged across `jleechanorg/agent-orchestrator-ts` #773/#752/#774 and `jleechanorg/agent-orchestrator` #21/#22/#23; bead closed with full proof trail including the no-self-merge human-facing-session rule). Branch protection fix: dropped `Skeptic Gate` from `agent-orchestrator-ts/main` required status checks (the actual root cause of why `#773` was unmergeable: it deletes the workflow that produces the check). Total 9 ironclad criteria met, harness boundary held throughout (bashrc `AO_CLI_PATH` untouched, all 4 `ai.agento.*` daemons at baseline state).

- Live Go AO cutover test on this Mac: swapped `~/.local/bin/ao` + `~/.local/bin/ao-go` to freshly-built post-merge Go binaries (from `/Users/jleechan/projects/agent-orchestrator/backend/cmd/ao`, HEAD `d2f59c86`); re-applied ad-hoc `codesign` because copying over existing signed-in-place binaries on macOS invalidates the original signature (the `com.apple.provenance` attribute issue); bootout'd launchd `ai.agento.ao-go-daemon` (PID 53403) and manually started the new daemon as a backgrounded process (`nohup ~/.local/bin/ao-go daemon > /tmp/ao-daemon-out.log 2> /tmp/ao-daemon-err.log &`, PID 76916).

- `ao doctor` showed all PASS after the cutover. Registered `mctrl-test` via `ao project add --id mctrl-test --path /Users/jleechan/.worktrees/mctrl-test --worker-agent codex` (after first `git clone`'ing the repo to that expected path since `~/.worktrees/mctrl-test` was missing). `ao spawn --project mctrl-test --harness codex ...` succeeded in returning "spawned session mctrl-test-1" + attached-tmux message, and the daemon correctly created a fresh worktree at `~/.ao/data/worktrees/mctrl-test/mctrl-test-1` on branch `ao/mctrl-test-1/root` — all plumbing works.

- **Live bug surfaced (not a passing test outcome)**: the spawned codex process exited silently. Pane was empty; daemon marked session `idle` instead of `exited`. Direct repro in the worktree: `codex --dangerously-bypass-hook-trust --dangerously-bypass-approvals 'echo hi'` exits 0 with stderr `error: unexpected argument '--dangerously-bypass-approvals' found ... a similar argument exists: '--dangerously-bypass-approvals-and-sandbox'`. The daemon's `internal/adapters/agent/codex/codex.go` (and likely `internal/adapters/reviewer/codex/codex.go`) was written against an older codex-cli where the flag was split; 0.144.5 combined them. Filed as [jleechan-dkh7](https://github.com/jleechanorg/agent-orchestrator/issues/) (P1, type=bug).

- **Full harness restore** after the test: `~/.bashrc` baseline-locked (AO_CLI_PATH line unchanged — never touched this session); `~/.local/bin/ao` and `~/.local/bin/ao-go` restored to pre-merge TS builds from `/tmp/baseline-ao-1784845515` and `/tmp/baseline-ao-go-1784845555` respectively, both re-codesigned; the manually-started Go daemon (PID 76916) killed; `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.agento.ao-go-daemon.plist` re-loaded (fresh PID 29823, normal launchd behavior); all 4 `ai.agento.*` launchd jobs back at baseline states (health-guardian, ao-go-daemon, novel-daily, health). `ao doctor` post-restore: all PASS with the legacy binaries. `/tmp/baseline-*` and `/tmp/ao-golang-*` snapshots preserved for forensics.

- **Open items (user-decision only)**: (1) fix [jleechan-dkh7](https://github.com/jleechanorg/agent-orchestrator/issues/) in upstream `AgentWrapper/agent-orchestrator` per fork policy and PR — recommended next step; (2) redo the trivial-PR live test on this Mac once the fix lands; (3) mirror to `/linux` jeff-ubuntu after the trivial-PR test passes; (4) clear or keep the `mctrl-test` project entry + 2 stale session rows in `~/.ao/data/ao.db`. No autonomous action on any of these.

- **Beads to close**: [jleechan-btiz](https://github.com/jleechanorg/agent-orchestrator-ts/issues/) (was the P1 resumption bead for the AO mission, superseded by [jleechan-1eds](https://github.com/jleechanorg/agent-orchestrator-ts/issues/) which is closed — closing `btiz` is an oversight to fix).
