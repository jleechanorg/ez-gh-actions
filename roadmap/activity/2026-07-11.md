# Activity — 2026-07-11

- **48-hour Mac/Linux stability audit and self-healing patch:** parallel read-only audits confirmed a forced-reset Mac outage amplified by false backend recovery and a Linux memory-pressure/watchdog reboot with continuing reconciliation churn. Opened [PR #56](https://github.com/jleechanorg/ez-gh-actions/pull/56) for platform-safe, Docker-health-verified restart with full process-group cleanup; local Rust suite 226/226 and clippy pass. Synced [`jleechan-jkgs` to issue #57](https://github.com/jleechanorg/ez-gh-actions/issues/57) and [`jleechan-5v8v` to issue #58](https://github.com/jleechanorg/ez-gh-actions/issues/58), completed durable memory + mem0 sync, and left production services undeployed. Full handoff: `/Users/jleechan/roadmap/nextsteps-2026-07-09-ezgha-runner-convergence.md`.
- **Dependency-ordered reliability queue correction:** updated the incident beads and rolling handoff after live verification found that factory activation/routing must precede cross-repo work, PR #56 is urgent phase-zero containment, PRs #52/#55 overlap while #54 is code-disjoint, and watchdogs must remain last. Added explicit `br` prerequisites for `ghd2.7`, `29v1`, and `lxn`; `br dep cycles` remains clean. Recorded a 16:03 PDT recurrence where unused Mac `~/.lima/colima` restarted while canonical Docker stayed on `~/.colima/default/docker.sock`. Full queue: `/Users/jleechan/roadmap/nextsteps-2026-07-09-ezgha-runner-convergence.md`.
- **Post-hoc review of the dependency-ordering + runner-queue-gate commits (evening):** verified ez-gh-actions `6a61b2f` and dark-factory PR [#248](https://github.com/jleechanorg/dark-factory/pull/248) commit `c011794` are beads-only, cycle-free, and all referenced beads exist. Found the `jleechan-x7ky` close ("verified all tests pass") contradicted by production: its fix (`29deb91f`) left `repo="${bead_repo:-$TARGET_REPO}"` with `TARGET_REPO` never assigned under `set -u`, crashing every scheduled `ai.dark-factory.af-tick` run for ~80 min (20 failures, 15:20–16:42 PDT) — filed [`jleechan-gvdw`](https://github.com/jleechanorg/ez-gh-actions) (P1) with fail-closed fix shape + scheduled-tick acceptance proof. The crash self-"resolved" only because another session switched the live daemon tree to `thin-skill-migration-f-factory` at 17:16 PDT; the bug persists on PR #248 head and blocks its merge. Current live tick failure is GraphQL rate-limit on `gh issue list` intake with no REST fallback — filed `jleechan-dhuo` (P2, dual-bucket fallback). Also blocked `ez-gh-actions-uh2` on `ez-gh-actions-30p` so the labeled duplicate stops appearing as dispatchable ready-work.
