### 2026-07-09 (ez-gh-actions) — Linux/macOS runner convergence root-cause and hardening lane

- `/nextsteps` refresh run at this checkpoint:
  - Recorded recent commit `1a02b36` (`codex/gpt-5: avoid backend restart churn when docker reachable`) and confirmed it does not obviate the P0 reconcile-race reopen.
  - Open critical blockers remain unchanged: `ez-gh-actions-5ki`, `ez-gh-actions-0jo`, `ez-gh-actions-lyf`.
  - `ez-gh-actions-4kv` still open for Mac+Linux synchronized deploy and full 22/22 continuous execution proof.

- Root-cause outcome: current deficit is a **reconciliation convergence failure**, not a single bad runner.
  - Linux host (`jeff-ubuntu`) observed at `13/16` active/online in cross-check against target despite no persistent GH label misrouting.
  - Mac host converged at current checks.
  - Alert/journal history on Linux showed repeated `ensure_count` partial-success cycles, `missing-registration`, `offline-respawning`, and reclaim/respawn churn.
- Primary finding: the stale-slot reclaim path can reclaim slots when `runner_id` is not yet visible in GH API, even when the local container is still alive and registration is in-flight.
- Beads opened:
  - `jleechan-zy6` — Investigate and fix Linux host reconciliation under churn (missing-registration + partial ensure_count).
  - `jleechan-9zd` — Implement local-container liveness guard before stale slot reclaim in `docker_backend`.
- Next steps:
  - Land Path 1 stability guard to avoid reclaiming alive containers during registration lag.
  - Keep the empty-id reservation fix (`jleechan-kbd`) and Linux in-flight reclaim guard (`jleechan-tku`) both active.
  - Re-soak Linux for 60m and verify cap stability (`16/16` sustained) and no reclaim/respawn oscillation.

- 2026-07-09 follow-up /nextsteps sync:
  - Closed `ez-gh-actions-o6i`.
  - Landed `codex/gpt-5: avoid backend restart churn when docker reachable` (`1a02b36`) on `main`.
  - P0 reconciliation blockers still open: `ez-gh-actions-5ki`, `ez-gh-actions-0jo`, `ez-gh-actions-lyf`.
  - `ez-gh-actions-4kv` remains open for cross-host (Linux + Mac) Gate-0-safe deploy sequencing.
